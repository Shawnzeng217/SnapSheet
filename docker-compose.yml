version: "3.8"

services:
  hotel-ocr:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./templates:/app/templates
      - ./output:/app/output
      - ./uploads:/app/uploads
    restart: unless-stopped
